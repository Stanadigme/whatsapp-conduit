# Dependencies
node_modules/

# Build output
dist/

# Test / coverage
coverage/
.vitest/

# Logs
*.log

# Sensitive runtime state — never commit (see AGENTS.md / README privacy model)
auth/
media/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
whatsapp-conduit.db*
# SQLite crash-recovery / journal artifacts (can contain private DB pages)
*-journal
*-shm
*-wal
config.yaml
config.yml
config.local.yaml
# Per-install HMAC key for export phone-number redaction — a secret.
redaction-salt
# Exported message data is private (see AGENTS.md): never commit exports.
exports/
*.jsonl
*.ndjson

# Local env
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
